{
  "name": "prompthub-ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --workspace=@prompthub/web",
    "build:web": "npm run build --workspace=@prompthub/web"
  },
  "workspaces": [
    "apps/*"
  ]
}
